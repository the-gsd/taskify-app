import { View, Text } from "react-native";
import React from "react";
import AddTaskComponent from "@/src/components/taskComponents/AddTaskComponent";

const addTask = () => {
  return <AddTaskComponent />;
};

export default addTask;
